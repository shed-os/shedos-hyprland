# wayland-wm@.service is uwsm's compositor template. Without this
# drop-in, its stderr (inherited from /dev/tty1) reaches the vcs
# buffer and gets painted by fbcon during DRM-master gaps.
[Service]
StandardOutput=journal
StandardError=journal
